"""Zone API — routes moved to /api/streams/{id}/zones (Phase C).

This module is kept as an empty placeholder so existing imports do not break.
"""
from fastapi import APIRouter

router = APIRouter()
